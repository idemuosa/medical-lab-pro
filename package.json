{
  "name": "medical-lab-pro-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend_next",
    "socket": "npm start --prefix socket_server",
    "django": "cd backend_django && py manage.py runserver",
    "start-all": "concurrently \"npm run dev --prefix frontend_next\" \"npm start --prefix socket_server\" \"cd backend_django && py manage.py runserver\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
